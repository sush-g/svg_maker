import keyMirror from 'keymirror';

export const WATERMARK_DESC = 'Created by svg_maker';

export const CANVAS_MODES = keyMirror({
  edit: null,
  preview: null
});

export const SVG_STROKE_LINECAP = keyMirror({
  butt: null,
  round: null,
  square: null
});
